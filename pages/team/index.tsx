import React from "react"

import RootLayout from "@/components/layout"

type Props = {}

const TeamPage = (props: Props) => {
	return (
		<RootLayout title="Team">
			<div>Team Page</div>
		</RootLayout>
	)
}

export default TeamPage
